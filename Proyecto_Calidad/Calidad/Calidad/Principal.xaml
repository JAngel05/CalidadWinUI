<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Calidad.Principal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calidad"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Principal">

    <Grid>
        <SplitView x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" IsPaneOpen="False" OpenPaneLength="200" CompactPaneLength="180" DisplayMode="CompactOverlay">
            <SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!--Menú-->
                        <RowDefinition Height="Auto"/>
                        <!--Usuarios-->
                        <RowDefinition Height="Auto"/>
                        <!--Productos-->
                    </Grid.RowDefinitions>

                    <TextBlock  Text="Menú" HorizontalAlignment="Left" Name="Header" Margin="10,10,10,10" ></TextBlock>
                    <Button Style="{StaticResource SubtleButtonStyle}" Grid.Row="1" Content="Usuarios" HorizontalAlignment="Left" Margin="10,10,10,10" Click="UsuariosBtn_Click"></Button>
                    <Button Style="{StaticResource SubtleButtonStyle}" Grid.Row="2" Content="Productos" HorizontalAlignment="Left" Margin="10,10,10,10" Click="ProductosBtn_Click"></Button>
                </Grid>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Window>
